Used Claude Sonet 4.6 to translate the Java Version into Javascript version with a simple prompt:

```
This is a Java Spring Open source project, i need to translate it to Raw(without using any frameworks, but some libs are allowed like Express) Javascript.

Also, the test case should be migrated.
The important is not the one to one translation,  but maintaining the same logic and domains.

Since the current app uses thymeleaf, the HTML would need some changes to use with Javascript.
```